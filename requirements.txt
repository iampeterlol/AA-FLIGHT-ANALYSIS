# Core data analysis
pandas>=2.0
numpy>=1.24

# Visualization
matplotlib>=3.7
seaborn>=0.12

# Machine learning / modeling
scikit-learn>=1.3
xgboost>=1.7          # Gradient boosting trees (tabular data, optional)
lightgbm>=4.0         # High-performance boosting model (optional)

# Notebook support
jupyter

# Config & environment
pyyaml                # Read/write YAML config files
python-dotenv         # Load environment variables from .env

# API & HTTP requests
requests              # Useful for calling APIs (METAR)

# Optional: install only if needed
# openpyxl            # Required only for Excel (.xlsx) files
# pyarrow             # Required only for Parquet file support